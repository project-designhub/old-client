import React from 'react';

const Loading = () => {
  return (
    <div
      style={{ display: 'flex', justifyContent: 'center', fontSize: '5rem' }}
    >
      ...Loading
    </div>
  );
};

export default Loading;
